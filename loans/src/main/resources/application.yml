spring:
  application:
    name: loans
  profiles:
    active: dev
  config:
    import: "optional:configserver:http://localhost:8888"
  cloud:
    config:
      username: configuser
      password: configpass